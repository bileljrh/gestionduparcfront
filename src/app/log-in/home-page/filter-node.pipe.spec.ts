import {FilterNodePipe} from './filter-node.pipe';

describe('FilterNodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNodePipe();
    expect(pipe).toBeTruthy();
  });
});
